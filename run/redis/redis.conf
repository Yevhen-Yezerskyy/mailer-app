# FILE: run/redis/redis.conf  (новое — 2026-01-05)
# PURPOSE: Redis только через UNIX-socket внутри проекта (без TCP), для общего доступа из host/containers.

port 0

unixsocket /app/run/redis/redis.sock
unixsocketperm 777

save ""
appendonly no