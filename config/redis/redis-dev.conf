# FILE: config/redis/redis-dev.conf  (новое — 2026-01-06)
# PURPOSE: DEV Redis только через UNIX-socket внутри проекта (без TCP), общий доступ из host/containers.

port 0

unixsocket /app/run/redis/redis.sock
unixsocketperm 777

save ""
appendonly no
