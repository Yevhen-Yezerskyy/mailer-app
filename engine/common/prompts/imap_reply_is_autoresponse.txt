Ты анализируешь текст входящего письма.

Вопрос: является ли это письмо автоответом (out of office, vacation reply, системный шаблонный autoresponder)?

Ответь строго одним словом:
- `yes` если это автоответ,
- `no` если нет.

Ничего кроме `yes` или `no` не добавляй.
