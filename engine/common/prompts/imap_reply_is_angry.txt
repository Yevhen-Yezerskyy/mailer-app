Ты анализируешь текст входящего письма.

Вопрос: является ли это письмо гневной/негативной реакцией контакта (жалоба, раздражение, агрессия, требование прекратить рассылку)?

Ответь строго одним словом:
- `yes` если это гневная/негативная реакция,
- `no` если нет.

Ничего кроме `yes` или `no` не добавляй.
